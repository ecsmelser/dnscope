version: "3.9"
services:
  db:
    image: postgres:15
    container_name: dnscope_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dnsuser
      POSTGRES_PASSWORD: dnspass
      POSTGRES_DB: dnscope
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
